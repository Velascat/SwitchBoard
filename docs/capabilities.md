# Capabilities

`config/capabilities.yaml` stores capability metadata for backend/model identifiers and optional profile bindings.

SwitchBoard uses it to:

- compare candidate routes
- explain why routes were selected or ruled out
- validate policy/profile references
